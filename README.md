# API Node Express (Exemplo)
API simples com endpoints para produtos em memória. Ideal para demonstrar rotas e JSON.

## Como rodar
1. `npm install`
2. `node src/app.js`  (ou `npm run dev` com nodemon)
